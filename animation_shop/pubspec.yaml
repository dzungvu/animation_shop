name: animation_shop
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.3
  shared_preferences: ^2.0.6
  get: ^4.3.8

  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - lib/res/assets/img1.jpeg
    - lib/res/assets/img2.jpeg
    - lib/res/assets/img3.jpeg
    - lib/res/assets/img4.jpeg
    - lib/res/assets/img5.jpeg
    - lib/res/assets/img6.jpeg
    - lib/res/assets/img7.jpeg
    - lib/res/assets/img8.jpeg
    - lib/res/assets/img9.jpeg
    - lib/res/assets/img10.jpeg

    - lib/res/assets/cardf.png
    - lib/res/assets/cardb.png
